# RUN: %build_test %s %t1 test_read_mesh.f90
# RUN: [[ -n "$MESH_FILES" ]] && ln -s "$MESH_FILES" . || exit 0
# RUN: [[ -n "$MESH_FILES" ]] && %mpirun -n 4 %t1

main: test_read_mesh
base: mpi
extra: parhip

