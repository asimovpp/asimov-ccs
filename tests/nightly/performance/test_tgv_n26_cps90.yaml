# RUN: %build_test %s %t1 
# RUN: export outputfile="output_tgv.txt"
# RUN: echo "date: $(date -Iminutes)" > $outputfile
# RUN: echo "ncores: 26" >> $outputfile
# RUN: echo "label: tgv_90" >> $outputfile
# RUN: echo "commitID: $(git rev-parse --short HEAD)" >> $outputfile
# RUN: echo "commitdate: $(git show -s --format=%cd --date=iso)" >> $outputfile
# RUN: echo "compiler: $CMP" >> $outputfile
# RUN: %mpirun -n 26 %t1 --ccs_case Input/TaylorGreenVortex --ccs_m 90 >> $outputfile

main: tgv
base: mpi_petsc
extra: parhip
