# Only run tests in this directory if the "NIGHTLY" environemnt variable is set
if config.environemnt["NIGHTLY"] == "true":
  # overwrite default test suffix to prevent picking up other config files as tests
  config.suffixes = ['.yml']
else:
  config.suffixes = ['']
