# RUN: %build_test %s %t1 
# RUN: CALI_CONFIG=print.metadata,runtime-report %mpirun -n 4 %t1 --ccs_case ../../src/case_setup/TaylorGreenVortex/TaylorGreenVortex --ccs_m 5 2>&1 | grep "cali.caliper.version"

main: tgv
base: mpi_petsc
options:
  profiler: profiler_caliper
extra: parhip
